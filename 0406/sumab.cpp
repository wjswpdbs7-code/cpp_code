#include <iostream>

int main() {
    int a, b;
    std::cout << "Enter two numbers: ";
    std::cin >> a >> b; // 사용자로부터 두 개의 정수를 입력받음
    int sum = a + b; // 입력받은 두 수의 합을 계산
    std::cout << "The sum of " << a << " and " << b << " is: " << sum << std::endl; // 결과 출력
    return 0;
}
//최대 
